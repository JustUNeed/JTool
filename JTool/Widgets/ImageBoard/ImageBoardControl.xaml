<UserControl x:Class="JTool.Widgets.ImageBoard.ImageBoardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>
    <ItemsControl ItemsSource="{Binding Items}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <WrapPanel/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Border Width="84" Height="84" Margin="3" Background="#FF3A3A3D">
                    <Grid>
                        <Button Background="Transparent" BorderThickness="0" Cursor="Hand"
                                ToolTip="点击复制图片"
                                Visibility="{Binding IsReady, Converter={StaticResource BoolToVis}}"
                                Command="{Binding DataContext.CopyCommand,
                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}">
                            <Image Source="{Binding Thumbnail}" Stretch="Uniform" Margin="4"/>
                        </Button>
                        <TextBlock Text="加载中" Foreground="#FF999999" FontSize="10"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="失败" Foreground="#FFE57373" FontSize="10"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"
                                   Visibility="{Binding IsFailed, Converter={StaticResource BoolToVis}}"/>
                        <Button Content="✕" Width="16" Height="16" FontSize="9"
                                HorizontalAlignment="Right" VerticalAlignment="Top" Margin="1"
                                Background="#88000000" Foreground="White" BorderThickness="0"
                                Cursor="Hand" ToolTip="删除"
                                Command="{Binding DataContext.RemoveCommand,
                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}"/>
                    </Grid>
                </Border>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
