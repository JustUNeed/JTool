<Window x:Class="JTool.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dd="urn:gong-wpf-dragdrop"
        Title="JTool 设置" Width="540" Height="560"
        WindowStartupLocation="CenterScreen" Background="#FF2D2D30">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="快捷项配置（可拖拽排序）"
                   Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding Shortcuts}"
                 Background="#FF1E1E1E" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 dd:DragDrop.IsDragSource="True"
                 dd:DragDrop.IsDropTarget="True"
                 dd:DragDrop.DropHandler="{Binding}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Margin" Value="0,2"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="#FF3A3A3D" CornerRadius="6" Padding="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Source="{Binding Icon}" Width="28" Height="28"
                                   Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,8,0">
                                <TextBox Text="{Binding Name, UpdateSourceTrigger=LostFocus}"
                                         Background="#FF2D2D30" Foreground="White"
                                         BorderBrush="#FF555555" Padding="4"/>
                                <TextBlock Text="{Binding Path}" Foreground="#FF888888" FontSize="10"
                                           TextTrimming="CharacterEllipsis" Margin="2,2,0,0"/>
                            </StackPanel>
                            <ComboBox Grid.Column="2" VerticalAlignment="Center" IsEditable="True"
                                      ItemsSource="{Binding DataContext.Groups, RelativeSource={RelativeSource AncestorType=Window}}"
                                      Text="{Binding Group, UpdateSourceTrigger=LostFocus}" Margin="0,0,8,0"/>
                            <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Content="↑" Width="26" Height="26" Margin="2,0" ToolTip="上移"
                                        Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="↓" Width="26" Height="26" Margin="2,0" ToolTip="下移"
                                        Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>
                                <Button Content="🗑" Width="26" Height="26" Margin="2,0" ToolTip="删除"
                                        Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <TextBox x:Name="NewGroupBox" Width="160" Padding="4"
                         Background="#FF2D2D30" Foreground="White" BorderBrush="#FF555555"
                         ToolTip="输入新分组名"/>
                <Button Content="新建分组" Margin="6,0,0,0" Padding="10,4" Click="AddGroup_Click"/>
            </StackPanel>
            <Button Grid.Column="1" Content="关闭" Padding="16,6" Click="Close_Click"/>
        </Grid>
    </Grid>
</Window>
